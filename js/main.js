(function(global){
	var DomainName = global.DomainName || (global.DomainName = {});
	var doc = global.document;
	
	debug.log('main.ls loaded');
	/*
	Class Defination.
	*/
})(this);

$(function(){
	/*
	Initialize on load.
	*/
});